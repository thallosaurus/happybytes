# npm-bytes-as-string

## How to use?

Import the Module like this in your NodeJS project:

``const bytes = require("./bytes.js"); ``

and use it like this:

``> bytes(85869345)
< [String: '85.87MB'] ``
